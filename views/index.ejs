<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title> K KEEPs </title>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Noto+Sans+JP:wght@100&family=Raleway:wght@100&display=swap');

        body{
            padding: 0%;
            margin: 0%;
            font-family: 'Noto Sans JP', sans-serif;
            background-color: aquamarine;
            display: flex;
            justify-content: center;
            align-items: center;
        }

        .box{
            margin-top: 100px;
            background-color: white;
            text-align: center;
            width: 700px;
            min-height: 70vh;
        }
        h1{
            font-size: 40px;
            padding: 20px;
        }
        ol{
            list-style: none;
        }
        .list{
            color: rgb(0, 0, 0);
            font-size: 20px;
            text-decoration: none;
            text-align: center;
        }
        input{
            background-color: aquamarine;
            border: none;
            padding: 10px;
            margin: 30px ;
        }

        button{
            text-decoration: none;
            border: none;
            background-color: rgb(83, 83, 237);
            color: white;
            font-size: 20px;
            margin: 30px 0px;
            padding: 5px;
            border-radius: 5px;
            transition: all ease-in-out 0.5s;
        }
        button:hover{
            background-color: white;
            color:rgb(83, 83, 237);
        }
    </style>
</head>
<body>

    <div class="box">
        <h1> <%= todayDate %> </h1>

        <form action="/" method="post" class="form">
            <ol>
                <% for (i= 0 ; i < newListItem.length ; i++) { %>
                    <li class="list"> <%= newListItem[i] %> </li>
                <% } %>
            </ol>
            <input type="text" name="newItem" class="input" autocomplete="off" placeholder="Enter Things TODO 👍" >
            <br>
            <button type="submit">ADD NOW</button>
        </form>

    </div>
</body>
</html>